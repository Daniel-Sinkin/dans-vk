# Vendored dans-core

This is a plain-file vendored copy of `dans-core`.

Source commit: 8476d1d

This directory is intentionally not a git submodule and is not consumed through
its own CMakeLists.txt. dans-util includes these headers privately.
